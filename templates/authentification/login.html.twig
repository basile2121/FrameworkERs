{% extends "base.html.twig" %}
{% block head %}
    {{ parent() }}
    <link rel="stylesheet" href="/css/profilUser.css"/>

{% endblock %}

{% block title %}Login{% endblock %}

{% block content %}
    <div class="container">
        {# Si registration est faite #}
        {% if successRegister %}
            <div class="alert alert-info m-auto mt-2 col-4">
                {{ successRegister }}
            </div>
        {% endif %}
        {# Si mauvais password #}
        {% if badPassword %}
            <div class="alert alert-danger m-auto mt-2 col-4">
                {{ badPassword }}
            </div>
        {% endif %}
        {# Si mauvais email #}
        {% if badEmail %}
            <div class="alert alert-danger m-auto mt-2 col-4">
                {{ badEmail }}
            </div>
        {% endif %}
        {# Si redirection car non enregistré #}
        {% if notLogged %}
            <div class="alert alert-danger m-auto mt-2 col-6">
                {{ notLogged }}
            </div>
        {% endif %}
        <section class="card event col-5 py-2 " style="margin: 20px auto;">
            <h1 class="m-auto">Connectez-vous</h1>

            <form action="/login" method="post" class="py-5">
                <div id="createEvent">
                    <div class="form-group">
                        <label for="email">Email
                        </label>
                        <input type="email" maxlength="255" name="email" class="form-control" id="email"
                               placeholder="email@example.com" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" maxlength="255" name="password" class="form-control" id="password"
                               placeholder="Password" required>
                    </div>
                </div>
                <div id="bouton">
                    <button type="submit" class="btn btn-success mt-3">Sign in</button>
                </div>
            </form>
            <p>Pas encore de compte ?
                <a href="/register" class="add btn btn-info text-white">S'enregistrer</a>
            </p>

        </section>
    </div>
{% endblock %}
